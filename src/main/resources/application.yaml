spring:
  application:
    name: bilanko
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
      active: ${SPRING_PROFILES_ACTIVE:local}
      
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_DURATION}